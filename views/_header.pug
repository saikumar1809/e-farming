header.header
  nav.nav.nav--tours
    a.nav__el(href='/') E FARMING
  .header__logo
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
    if user
      button.nav__el.nav__el--logout.logOutBtn logout;
      a.nav__el(href='/me')
        span= user.name
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='/signup') Sign up